<style>
    /* Navbar Glassmorphism Effect */
    .navbar-glass {
        background: linear-gradient(135deg, rgba(8, 122, 41, 0.95) 0%, rgba(6, 90, 31, 0.98) 100%);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
    }
    
    /* Animated gradient border */
    .navbar-border-glow {
        position: relative;
    }
    
    .navbar-border-glow::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #F9B233, #ffc107, #F9B233, #d4962b, #F9B233);
        background-size: 200% 100%;
        animation: shimmer 3s ease-in-out infinite;
    }
    
    @keyframes shimmer {
        0%, 100% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
    }
    
    /* Logo pulse animation */
    .logo-pulse {
        animation: logoPulse 2s ease-in-out infinite;
    }
    
    @keyframes logoPulse {
        0%, 100% { box-shadow: 0 0 0 0 rgba(249, 178, 51, 0.4); }
        50% { box-shadow: 0 0 20px 5px rgba(249, 178, 51, 0.2); }
    }
    
    /* Nav link hover effect */
    .nav-link-enhanced {
        position: relative;
        overflow: hidden;
    }
    
    .nav-link-enhanced::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
        transition: left 0.5s ease;
    }
    
    .nav-link-enhanced:hover::before {
        left: 100%;
    }
    
    /* User button glow */
    .user-btn-glow {
        transition: all 0.3s ease;
    }
    
    .user-btn-glow:hover {
        box-shadow: 0 0 25px rgba(249, 178, 51, 0.3);
    }
    
    /* Floating animation for decorative elements */
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-5px); }
    }
    
    .float-animation {
        animation: float 3s ease-in-out infinite;
    }
</style>

<nav class="navbar-glass navbar-border-glow sticky top-0 z-50 shadow-xl" id="mainNavbar">
    <!-- Decorative background particles -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-2 left-[10%] w-2 h-2 bg-clsu-gold/20 rounded-full float-animation"></div>
        <div class="absolute top-4 left-[30%] w-1.5 h-1.5 bg-white/10 rounded-full float-animation" style="animation-delay: 0.5s;"></div>
        <div class="absolute top-3 right-[20%] w-2 h-2 bg-clsu-gold/15 rounded-full float-animation" style="animation-delay: 1s;"></div>
        <div class="absolute top-5 right-[40%] w-1 h-1 bg-white/20 rounded-full float-animation" style="animation-delay: 1.5s;"></div>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
        <div class="flex items-center justify-between h-20 lg:h-24">
            
            <!-- Logo with enhanced styling -->
            <a href="{{ route('home') }}" class="flex-shrink-0 group flex items-center gap-4">
                <div class="relative">
                    <!-- Glow effect behind logo -->
                    <div class="absolute inset-0 bg-gradient-to-r from-clsu-gold/30 to-yellow-300/30 rounded-full blur-xl 
                                group-hover:from-clsu-gold/50 group-hover:to-yellow-300/50 transition-all duration-500 scale-110"></div>
                    <img src="{{ asset('inspinia/img/landing/logo.png') }}" 
                         alt="CLSU ETEEAP Logo" 
                         class="relative h-16 w-16 lg:h-20 lg:w-20 rounded-full bg-white p-1.5 border-[3px] border-clsu-gold 
                                shadow-lg logo-pulse transform group-hover:scale-110 group-hover:rotate-3 transition-all duration-300">
                </div>
                <!-- Brand text - visible on larger screens -->
                <div class="hidden xl:block">
                    <h1 class="text-white font-bold text-lg leading-tight">CLSU</h1>
                    <p class="text-clsu-gold text-sm font-semibold tracking-wider">ETEEAP</p>
                </div>
            </a>

            <!-- Desktop Navigation with enhanced styling -->
            <div class="hidden lg:flex items-center space-x-1 xl:space-x-2">
                <a href="{{ url('/') }}" 
                   class="nav-link-enhanced group relative px-4 py-2.5 text-sm xl:text-base font-semibold text-white hover:text-clsu-gold transition-all duration-300 rounded-lg hover:bg-white/5
                          {{ request()->is('/') ? 'text-clsu-gold bg-white/10' : '' }}">
                    <span class="relative z-10 flex items-center gap-2">
                        <i class="fas fa-home text-xs opacity-0 group-hover:opacity-100 transition-opacity duration-300"></i>
                        Home
                    </span>
                    <span class="absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-gradient-to-r from-transparent via-clsu-gold to-transparent transition-all duration-300 group-hover:w-full
                                {{ request()->is('/') ? 'w-full' : '' }}"></span>
                </a>
                
                <a href="{{ route('mission.vision') }}" 
                   class="nav-link-enhanced group relative px-4 py-2.5 text-sm xl:text-base font-semibold text-white hover:text-clsu-gold transition-all duration-300 rounded-lg hover:bg-white/5
                          {{ request()->routeIs('mission.vision') ? 'text-clsu-gold bg-white/10' : '' }}">
                    <span class="relative z-10 flex items-center gap-2">
                        <i class="fas fa-bullseye text-xs opacity-0 group-hover:opacity-100 transition-opacity duration-300"></i>
                        Mission & Vision
                    </span>
                    <span class="absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-gradient-to-r from-transparent via-clsu-gold to-transparent transition-all duration-300 group-hover:w-full
                                {{ request()->routeIs('mission.vision') ? 'w-full' : '' }}"></span>
                </a>
                
                <a href="{{ route('admission.requirements') }}" 
                   class="nav-link-enhanced group relative px-4 py-2.5 text-sm xl:text-base font-semibold text-white hover:text-clsu-gold transition-all duration-300 rounded-lg hover:bg-white/5
                          {{ request()->routeIs('admission.requirements') ? 'text-clsu-gold bg-white/10' : '' }}">
                    <span class="relative z-10 flex items-center gap-2">
                        <i class="fas fa-file-alt text-xs opacity-0 group-hover:opacity-100 transition-opacity duration-300"></i>
                        Admission
                    </span>
                    <span class="absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-gradient-to-r from-transparent via-clsu-gold to-transparent transition-all duration-300 group-hover:w-full
                                {{ request()->routeIs('admission.requirements') ? 'w-full' : '' }}"></span>
                </a>
                
                <a href="{{ route('qualification') }}" 
                   class="nav-link-enhanced group relative px-4 py-2.5 text-sm xl:text-base font-semibold text-white hover:text-clsu-gold transition-all duration-300 rounded-lg hover:bg-white/5
                          {{ request()->routeIs('qualification') ? 'text-clsu-gold bg-white/10' : '' }}">
                    <span class="relative z-10 flex items-center gap-2">
                        <i class="fas fa-check-circle text-xs opacity-0 group-hover:opacity-100 transition-opacity duration-300"></i>
                        Qualification
                    </span>
                    <span class="absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-gradient-to-r from-transparent via-clsu-gold to-transparent transition-all duration-300 group-hover:w-full
                                {{ request()->routeIs('qualification') ? 'w-full' : '' }}"></span>
                </a>
                
                <a href="{{ route('procedures') }}" 
                   class="nav-link-enhanced group relative px-4 py-2.5 text-sm xl:text-base font-semibold text-white hover:text-clsu-gold transition-all duration-300 rounded-lg hover:bg-white/5
                          {{ request()->routeIs('procedures') ? 'text-clsu-gold bg-white/10' : '' }}">
                    <span class="relative z-10 flex items-center gap-2">
                        <i class="fas fa-list-ol text-xs opacity-0 group-hover:opacity-100 transition-opacity duration-300"></i>
                        Procedures
                    </span>
                    <span class="absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-gradient-to-r from-transparent via-clsu-gold to-transparent transition-all duration-300 group-hover:w-full
                                {{ request()->routeIs('procedures') ? 'w-full' : '' }}"></span>
                </a>
                
                <a href="{{ route('courses.offered') }}" 
                   class="nav-link-enhanced group relative px-4 py-2.5 text-sm xl:text-base font-semibold text-white hover:text-clsu-gold transition-all duration-300 rounded-lg hover:bg-white/5
                          {{ request()->routeIs('courses.offered') ? 'text-clsu-gold bg-white/10' : '' }}">
                    <span class="relative z-10 flex items-center gap-2">
                        <i class="fas fa-graduation-cap text-xs opacity-0 group-hover:opacity-100 transition-opacity duration-300"></i>
                        Courses
                    </span>
                    <span class="absolute bottom-0 left-1/2 -translate-x-1/2 w-0 h-0.5 bg-gradient-to-r from-transparent via-clsu-gold to-transparent transition-all duration-300 group-hover:w-full
                                {{ request()->routeIs('courses.offered') ? 'w-full' : '' }}"></span>
                </a>
            </div>

            <!-- Auth Buttons / User Menu with enhanced styling -->
            <div class="hidden lg:flex items-center space-x-3">
                @guest
                    <a href="{{ route('login') }}" 
                       class="group inline-flex items-center px-5 py-2.5 bg-white/10 hover:bg-white/20 text-white font-semibold rounded-xl 
                              border border-white/20 hover:border-clsu-gold/50 transition-all duration-300 backdrop-blur-sm
                              hover:shadow-lg hover:shadow-white/10">
                        <i class="fas fa-sign-in-alt mr-2 group-hover:translate-x-0.5 transition-transform duration-300"></i>
                        Login
                    </a>
                    @if (Route::has('register'))
                        <a href="{{ route('register') }}" 
                           class="group inline-flex items-center px-5 py-2.5 bg-gradient-to-r from-clsu-gold to-yellow-400 hover:from-yellow-400 hover:to-clsu-gold 
                                  text-clsu-green-dark font-bold rounded-xl shadow-lg hover:shadow-xl hover:shadow-clsu-gold/30
                                  transform hover:-translate-y-0.5 transition-all duration-300">
                            <i class="fas fa-user-plus mr-2 group-hover:scale-110 transition-transform duration-300"></i>
                            Register
                            <span class="ml-2 w-2 h-2 bg-clsu-green rounded-full animate-pulse"></span>
                        </a>
                    @endif
                @else
                    <!-- Enhanced User Dropdown -->
                    <div class="relative" x-data="{ open: false }">
                        <button @click="open = !open" 
                                class="user-btn-glow flex items-center space-x-3 px-4 py-2.5 bg-gradient-to-r from-white/10 to-white/5 hover:from-white/20 hover:to-white/10 
                                       rounded-xl border border-white/20 hover:border-clsu-gold/50 transition-all duration-300">
                            <!-- Animated avatar -->
                            <div class="relative">
                                <div class="w-10 h-10 bg-gradient-to-br from-clsu-gold to-yellow-300 rounded-full flex items-center justify-center 
                                            border-2 border-white/30 shadow-lg">
                                    <span class="text-clsu-green-dark font-bold text-sm">
                                        {{ strtoupper(substr(Auth::user()->first_name ?? Auth::user()->name, 0, 1)) }}{{ strtoupper(substr(Auth::user()->surname ?? '', 0, 1)) }}
                                    </span>
                                </div>
                                <!-- Online indicator -->
                                <span class="absolute bottom-0 right-0 w-3 h-3 bg-green-400 rounded-full border-2 border-clsu-green animate-pulse"></span>
                            </div>
                            <div class="text-left hidden xl:block">
                                <p class="text-white font-semibold text-sm max-w-[100px] truncate">{{ Auth::user()->first_name ?? Auth::user()->name }}</p>
                                <p class="text-white/60 text-xs">Applicant</p>
                            </div>
                            <i class="fas fa-chevron-down text-white/70 text-xs transition-transform duration-300" 
                               :class="{ 'rotate-180': open }"></i>
                        </button>
                        
                        <!-- Enhanced Dropdown Menu -->
                        <div x-show="open"
                             x-cloak
                             x-transition:enter="transition ease-out duration-200"
                             x-transition:enter-start="opacity-0 translate-y-2 scale-95"
                             x-transition:enter-end="opacity-100 translate-y-0 scale-100"
                             x-transition:leave="transition ease-in duration-150"
                             x-transition:leave-start="opacity-100 translate-y-0 scale-100"
                             x-transition:leave-end="opacity-0 translate-y-2 scale-95"
                             @click.away="open = false"
                             class="absolute right-0 mt-3 w-64 bg-white rounded-2xl shadow-2xl border border-gray-100 overflow-hidden z-50">
                            
                            <!-- User Info Header -->
                            <div class="p-5 bg-gradient-to-br from-clsu-green via-clsu-green to-clsu-green-dark relative overflow-hidden">
                                <div class="absolute top-0 right-0 w-20 h-20 bg-clsu-gold/10 rounded-full -translate-y-1/2 translate-x-1/2"></div>
                                <div class="relative flex items-center gap-3">
                                    <div class="w-12 h-12 bg-gradient-to-br from-clsu-gold to-yellow-300 rounded-full flex items-center justify-center 
                                                border-2 border-white/30 shadow-lg">
                                        <span class="text-clsu-green-dark font-bold">
                                            {{ strtoupper(substr(Auth::user()->first_name ?? Auth::user()->name, 0, 1)) }}{{ strtoupper(substr(Auth::user()->surname ?? '', 0, 1)) }}
                                        </span>
                                    </div>
                                    <div>
                                        <p class="text-white font-bold truncate">{{ Auth::user()->first_name ?? Auth::user()->name }} {{ Auth::user()->surname ?? '' }}</p>
                                        <p class="text-white/70 text-sm truncate">{{ Auth::user()->email }}</p>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Menu Items -->
                            <div class="py-2">
                                <a href="{{ route('user.index', Auth::user()->id) }}" 
                                   class="group flex items-center px-5 py-3 text-gray-700 hover:bg-gradient-to-r hover:from-clsu-green/5 hover:to-transparent transition-all duration-200">
                                    <div class="w-9 h-9 bg-clsu-green/10 rounded-lg flex items-center justify-center group-hover:bg-clsu-green group-hover:text-white transition-all duration-200">
                                        <i class="fas fa-file-alt text-clsu-green group-hover:text-white"></i>
                                    </div>
                                    <div class="ml-3">
                                        <span class="font-semibold text-gray-800 group-hover:text-clsu-green">My Applications</span>
                                        <p class="text-xs text-gray-500">View & manage applications</p>
                                    </div>
                                </a>
                                <a href="{{ route('profile.edit') }}" 
                                   class="group flex items-center px-5 py-3 text-gray-700 hover:bg-gradient-to-r hover:from-clsu-green/5 hover:to-transparent transition-all duration-200">
                                    <div class="w-9 h-9 bg-clsu-gold/10 rounded-lg flex items-center justify-center group-hover:bg-clsu-gold transition-all duration-200">
                                        <i class="fas fa-user-edit text-clsu-gold group-hover:text-white"></i>
                                    </div>
                                    <div class="ml-3">
                                        <span class="font-semibold text-gray-800 group-hover:text-clsu-green">Edit Profile</span>
                                        <p class="text-xs text-gray-500">Update your information</p>
                                    </div>
                                </a>
                            </div>
                            
                            <!-- Logout -->
                            <div class="border-t border-gray-100 p-2">
                                <form method="POST" action="{{ route('logout') }}">
                                    @csrf
                                    <button type="submit" 
                                            class="group flex items-center w-full px-4 py-3 rounded-xl text-red-600 hover:bg-red-50 transition-all duration-200">
                                        <div class="w-9 h-9 bg-red-100 rounded-lg flex items-center justify-center group-hover:bg-red-500 transition-all duration-200">
                                            <i class="fas fa-sign-out-alt text-red-500 group-hover:text-white"></i>
                                        </div>
                                        <span class="ml-3 font-semibold">Logout</span>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                @endguest
            </div>

            <!-- Mobile Menu Button with animation -->
            <button type="button" 
                    class="lg:hidden inline-flex items-center justify-center p-3 rounded-xl text-white bg-white/10 hover:bg-white/20 
                           border border-white/20 transition-all duration-300 hover:border-clsu-gold/50"
                    onclick="toggleMobileMenu()">
                <svg class="h-6 w-6 transition-transform duration-300" id="menuIconOpen" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
                <svg class="h-6 w-6 hidden transition-transform duration-300" id="menuIconClose" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
            </button>
        </div>
    </div>

    <!-- Enhanced Mobile Menu -->
    <div class="lg:hidden hidden" id="mobileMenu">
        <div class="px-4 pt-4 pb-6 space-y-2 bg-gradient-to-b from-clsu-green-dark/98 to-clsu-green/98 backdrop-blur-xl border-t border-white/10">
            
            <!-- Mobile Navigation Links -->
            <a href="{{ url('/') }}" 
               class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200
                      {{ request()->is('/') ? 'bg-white/15 text-clsu-gold border-l-4 border-clsu-gold' : '' }}">
                <div class="w-9 h-9 bg-white/10 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-home {{ request()->is('/') ? 'text-clsu-gold' : 'text-white/70' }}"></i>
                </div>
                <span>Home</span>
            </a>
            
            <a href="{{ route('mission.vision') }}" 
               class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200
                      {{ request()->routeIs('mission.vision') ? 'bg-white/15 text-clsu-gold border-l-4 border-clsu-gold' : '' }}">
                <div class="w-9 h-9 bg-white/10 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-bullseye {{ request()->routeIs('mission.vision') ? 'text-clsu-gold' : 'text-white/70' }}"></i>
                </div>
                <span>Mission & Vision</span>
            </a>
            
            <a href="{{ route('admission.requirements') }}" 
               class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200
                      {{ request()->routeIs('admission.requirements') ? 'bg-white/15 text-clsu-gold border-l-4 border-clsu-gold' : '' }}">
                <div class="w-9 h-9 bg-white/10 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-file-alt {{ request()->routeIs('admission.requirements') ? 'text-clsu-gold' : 'text-white/70' }}"></i>
                </div>
                <span>Admission Requirements</span>
            </a>
            
            <a href="{{ route('qualification') }}" 
               class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200
                      {{ request()->routeIs('qualification') ? 'bg-white/15 text-clsu-gold border-l-4 border-clsu-gold' : '' }}">
                <div class="w-9 h-9 bg-white/10 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-check-circle {{ request()->routeIs('qualification') ? 'text-clsu-gold' : 'text-white/70' }}"></i>
                </div>
                <span>Qualification</span>
            </a>
            
            <a href="{{ route('procedures') }}" 
               class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200
                      {{ request()->routeIs('procedures') ? 'bg-white/15 text-clsu-gold border-l-4 border-clsu-gold' : '' }}">
                <div class="w-9 h-9 bg-white/10 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-list-ol {{ request()->routeIs('procedures') ? 'text-clsu-gold' : 'text-white/70' }}"></i>
                </div>
                <span>Procedures</span>
            </a>
            
            <a href="{{ route('courses.offered') }}" 
               class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200
                      {{ request()->routeIs('courses.offered') ? 'bg-white/15 text-clsu-gold border-l-4 border-clsu-gold' : '' }}">
                <div class="w-9 h-9 bg-white/10 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-graduation-cap {{ request()->routeIs('courses.offered') ? 'text-clsu-gold' : 'text-white/70' }}"></i>
                </div>
                <span>Courses Offered</span>
            </a>
            
            <!-- Mobile Auth Section -->
            <div class="pt-4 mt-4 border-t border-white/10 space-y-3">
                @guest
                    <a href="{{ route('login') }}" 
                       class="flex items-center justify-center px-4 py-3.5 bg-white/10 rounded-xl text-white font-semibold hover:bg-white/20 transition-all duration-200 border border-white/20">
                        <i class="fas fa-sign-in-alt mr-2"></i>
                        Login
                    </a>
                    @if (Route::has('register'))
                        <a href="{{ route('register') }}" 
                           class="flex items-center justify-center px-4 py-3.5 bg-gradient-to-r from-clsu-gold to-yellow-400 rounded-xl text-clsu-green-dark font-bold hover:from-yellow-400 hover:to-clsu-gold transition-all duration-200 shadow-lg">
                            <i class="fas fa-user-plus mr-2"></i>
                            Register Now
                        </a>
                    @endif
                @else
                    <!-- User Card -->
                    <div class="flex items-center p-4 bg-gradient-to-r from-white/10 to-white/5 rounded-xl border border-white/20">
                        <div class="relative">
                            <div class="w-12 h-12 bg-gradient-to-br from-clsu-gold to-yellow-300 rounded-full flex items-center justify-center border-2 border-white/30">
                                <span class="text-clsu-green-dark font-bold">
                                    {{ strtoupper(substr(Auth::user()->first_name ?? Auth::user()->name, 0, 1)) }}{{ strtoupper(substr(Auth::user()->surname ?? '', 0, 1)) }}
                                </span>
                            </div>
                            <span class="absolute bottom-0 right-0 w-3 h-3 bg-green-400 rounded-full border-2 border-clsu-green"></span>
                        </div>
                        <div class="ml-3">
                            <p class="text-white font-semibold">{{ Auth::user()->first_name ?? Auth::user()->name }} {{ Auth::user()->surname ?? '' }}</p>
                            <p class="text-white/60 text-sm">{{ Auth::user()->email }}</p>
                        </div>
                    </div>
                    
                    <a href="{{ route('user.index', Auth::user()->id) }}" 
                       class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200">
                        <div class="w-9 h-9 bg-clsu-gold/20 rounded-lg flex items-center justify-center mr-3">
                            <i class="fas fa-file-alt text-clsu-gold"></i>
                        </div>
                        <span>My Applications</span>
                    </a>
                    
                    <a href="{{ route('profile.edit') }}" 
                       class="flex items-center px-4 py-3.5 rounded-xl text-white font-medium hover:bg-white/10 transition-all duration-200">
                        <div class="w-9 h-9 bg-clsu-gold/20 rounded-lg flex items-center justify-center mr-3">
                            <i class="fas fa-user-edit text-clsu-gold"></i>
                        </div>
                        <span>Edit Profile</span>
                    </a>
                    
                    <form method="POST" action="{{ route('logout') }}">
                        @csrf
                        <button type="submit" 
                                class="flex items-center w-full px-4 py-3.5 rounded-xl text-red-400 font-medium hover:bg-red-500/10 transition-all duration-200">
                            <div class="w-9 h-9 bg-red-500/20 rounded-lg flex items-center justify-center mr-3">
                                <i class="fas fa-sign-out-alt text-red-400"></i>
                            </div>
                            <span>Logout</span>
                        </button>
                    </form>
                @endguest
            </div>
        </div>
    </div>
</nav>

<script>
    function toggleMobileMenu() {
        const mobileMenu = document.getElementById('mobileMenu');
        const menuIconOpen = document.getElementById('menuIconOpen');
        const menuIconClose = document.getElementById('menuIconClose');
        
        mobileMenu.classList.toggle('hidden');
        menuIconOpen.classList.toggle('hidden');
        menuIconClose.classList.toggle('hidden');
        
        // Add rotation animation
        if (!mobileMenu.classList.contains('hidden')) {
            menuIconClose.style.transform = 'rotate(180deg)';
        } else {
            menuIconClose.style.transform = 'rotate(0deg)';
        }
    }
    
    // Enhanced navbar scroll effect
    let lastScroll = 0;
    window.addEventListener('scroll', function() {
        const navbar = document.getElementById('mainNavbar');
        const currentScroll = window.pageYOffset;
        
        if (currentScroll > 50) {
            navbar.classList.add('shadow-2xl');
            navbar.style.background = 'linear-gradient(135deg, rgba(8, 122, 41, 0.98) 0%, rgba(6, 90, 31, 1) 100%)';
        } else {
            navbar.classList.remove('shadow-2xl');
            navbar.style.background = '';
        }
        
        lastScroll = currentScroll;
    });
</script>